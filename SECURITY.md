# 安全策略

## 报告漏洞

请不要在公开 Issue、讨论区或社交媒体发布尚未修复的漏洞细节、凭证或可复现的真实用户数据。

如果仓库启用了 GitHub Private Vulnerability Reporting，请优先通过该渠道报告；否则请联系项目维护者并要求使用私下渠道沟通。报告至少应包含：

- 受影响的版本、提交或运行方式；
- 复现步骤和最小化示例；
- 影响范围（例如本地数据泄露、权限绕过或拒绝服务）；
- 可能的修复建议（如有）。

当前项目没有承诺固定响应时间或公开的受支持版本列表。维护者会在能够确认问题后更新处理状态；请给出足够时间完成修复和发布说明。

## 凭证与本地数据

- `GITHUB_TOKEN` 只用于提高 GitHub 只读 API 的限流额度，应使用最小权限。
- `.env`、`.env.local`、SQLite 数据库、导出的 Markdown/JSON/OPML 和日志都可能包含敏感信息，不应提交或粘贴到公开报告中。
- AI 梳理页的 BYOK API Key 保存在当前浏览器的 `localStorage`，能访问该浏览器配置的脚本或用户可能读取它；不要在公共电脑上长期保存，使用后应清除。
- 发现凭证泄露时，应先立即撤销或轮换凭证，再报告泄露位置；不要把有效凭证复制进 Issue 或补丁。
- RepoTrellis 默认是本地单用户应用，不等同于经过身份认证和多租户隔离的生产服务。

## 安全边界

项目遵循官方 API/RSS 优先、最小权限、限流、超时和来源留痕原则。应用不会为了导入内容而绕过验证码、反爬、访问控制或第三方登录；如果发现连接器违反这些边界，请按上面的私下渠道报告。
